#include <stdio.h>
void main()
{
    int i, j;
    int a[10];
    printf("enter the number \n");
    scanf("%d\n", &a[i]);
    for (i = 0; i < a[i]; i++)
    {
        if (a[i] != 0 && a[i] != 1)
        {
            printf("invalid");
        }
        else
        {
            printf("valid");
        }
    }
}