#include<stdio.h>
#include <stdio.h>
void main()
{
    int a, b, c, d, e, f, g, h, i, j;
    int averagemarks,z; 
    //char  str[min],str[max];
    printf("enter the marks of student \n");
    scanf("%d %d %d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h, &i, &j);
    averagemarks = (a + b + c + d + e + f + g + h + i + j) / 10;
    printf("%d \n", averagemarks);
    marks=a, b, c, d, e, f, g, h, i, j;
    for(z=1;zz++)
    {
        if(z<marks)
    }
}